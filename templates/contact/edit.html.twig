{% extends 'base.html.twig' %}

{% block title %}Editer le contact{% endblock %}

{% block body %}
    <h1>Editer le contact</h1>

    {{ include('contact/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_contact_index') }}">Retour à la liste de contacts</a>

    {{ include('contact/_delete_form.html.twig') }}
{% endblock %}
