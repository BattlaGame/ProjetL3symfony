{% extends 'base.html.twig' %}

{% block title %}Poste{% endblock %}

{% block body %}
    <h1>Poste</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ poste.id }}</td>
            </tr>
            <tr>
                <th>NamePoste</th>
                <td>{{ poste.namePoste }}</td>
            </tr>
            {% for adherent in poste.adherent %}
            <tr>
                <th>Joueur</th>
                <td>{{ adherent.user.getName() }}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_poste_index') }}">Retour à la liste</a>

    <a href="{{ path('app_poste_edit', {'id': poste.id}) }}">Editer</a>

    {{ include('poste/_delete_form.html.twig') }}
{% endblock %}
