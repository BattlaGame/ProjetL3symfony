{% extends 'base.html.twig' %}

{% block title %}Page d'accueil{% endblock %}

{% block body %}
    <h1>Joué Volley Ball</h1>

    <table class="table">
        <thead>
            <nav>
                <ul>
                    <li class="deroulant">
                    <a href="{{ path('app_home_index') }}">Accueil</a>
                        <ul class="sous">
                            <li>
                            <a href="{{ path('app_home_index') }}">Accueil</a>
                            </li>
                        </ul>
                    </li>

                    <li class="deroulant">
                    <a href="{{ path('app_compte_index') }}">Compte</a>
                        <ul class="sous">
                            <li>
                            <a href="{{ path('app_compte_new') }}">Création de compte</a>
                            </li>
                            <li>
                            <a href="{{ path('app_compte_connexion') }}">Connexion</a>
                            </li>
                        </ul>
                    </li>
                    <li><a href="{{ path('app_home_newsletter') }}">Newsletters</a></li>
                    <li><a href="{{ path('app_home_contact') }}">Contact</a></li>
                </ul>
            </nav>
        </thead>
        <tbody>
            <p>Les club de volley de Joué les Tours est super cool du coup venez.</p>
            
      {#  {% for home in homes %}
            <tr>
                <td>{{ home.id }}</td>
                <td>
                    <a href="{{ path('app_home_show', {'id': home.id}) }}">show</a>
                    <a href="{{ path('app_home_edit', {'id': home.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">Erreur</td>
            </tr>
        {% endfor %} #}
        </tbody>
    </table>

{% endblock %}
